/// Integration test for {{project-name}}.
///
/// These tests exercise the public API from a consumer's perspective.

use {{crate_name}};

#[test]
fn test_crate_links_and_compiles() {
    // This test merely verifies that the crate compiles and links correctly
    // from the consumer (integration test) perspective.
    assert!(true, "Integration test infrastructure works");
}